// import request from "supertest";
// import createServer from "../createServer";

describe("/books endpoint", () => {
  describe("when POST /book", () => {
    it("should response 201 and persisted new thread", (done) => {
      // // Arrange
      // const payload = {
      //   bookName: "Judul Buku",
      // };

      // const response = await request(createServer).post("/book").send(payload);

      // // Assert
      // expect(response.statusCode).toEqual(201);

      done();
    });
  });
});
